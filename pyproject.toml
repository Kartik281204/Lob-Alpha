[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: end-to-end smoke tests that run the full pipeline (deselect with -m 'not slow')",
]

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]
